/**
 * React Native CLI config.
 * `react-native-asset` reads `assets` to copy the fonts into native projects.
 */
module.exports = {
  project: {
    ios: {},
    android: {},
  },
  assets: ['./assets/fonts/'],
};
